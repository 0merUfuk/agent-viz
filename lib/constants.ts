/**
 * Hardcoded brand identity for the cinematic demo. The visualization always
 * presents itself as the SedatSencan/age-of-ai project regardless of which
 * ecosystem JSON is loaded — the audience sees one brand, always.
 *
 * Single source of truth. Do not introduce parallel constants elsewhere.
 */
export const SOURCE_LABEL = "SedatSencan/age-of-ai";
